export const sum = (num1, num2) => num1 + num2;

export const sub = (num1, num2) => num1 - num2;

export const mult = (num1, num2) => num1 * num2;

export const div  = (num1, num2) => num2 === 0 ? 'Não é possível dividir por zero!': num1 / num2;